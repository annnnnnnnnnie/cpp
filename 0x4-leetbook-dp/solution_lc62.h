// LC 62

#include <vector>
#include <algorithm>
class SolutionLC62 {
public:
  int uniquePaths(int m, int n) {
    std::vector<std::vector<int>> paths_table(m, std::vector<int>(n, 0));

    for(std::vector<int>::size_type i = 0; i < paths_table.size(); i++){
      
    }
    return 0;
  }
};